<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="icon" href="/favicon.ico" />
    <meta name="generator" content={Astro.generator} />
    <title>Martin Larsson — Software Developer</title>
    <meta
      name="description"
      content="Portfolio website for Martin Larsson, a software developer working across systems, web, and modern tooling."
    />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Mono:wght@400;500;600&family=Inter:wght@400;500;600&display=swap"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="page-shell">
      <slot />
    </div>
  </body>
</html>

<style is:global>
  :root {
    --bg: #0b0d10;
    --ink: #edeae3;
    --ink-dim: #868c97;
    --accent: #e8a33d;
    --rule: #1d2026;
  }

  html,
  body {
    margin: 0;
    width: 100%;
    min-height: 100%;
    background: var(--bg);
    color: var(--ink);
    font-family: "Manrope", sans-serif;
  }

  a {
    color: inherit;
  }
  .post-date {
    margin: 0 0 0.5rem;
    color: var(--accent);
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.8rem;
  }
  .page-shell {
    max-width: 72rem;
    margin: 0 auto;
    padding-bottom: 3rem;
  }

  .hero {
    margin-bottom: 2rem;
  }

  .eyebrow {
    font-family: "IBM Plex Mono", monospace;
    font-size: 0.8rem;
    letter-spacing: 0.08em;
    color: var(--accent);
    margin: 0 0 0.75rem;
  }

  h1 {
    font-family: "IBM Plex Mono", monospace;
    font-size: clamp(2rem, 3.6vw, 2.8rem);
    margin: 0 0 0.75rem;
    color: var(--ink);
  }

  .intro {
    color: var(--ink-dim);
    font-size: 1rem;
    line-height: 1.7;
    max-width: 42rem;
    margin: 0;
  }

  .post-list {
    display: grid;
    gap: 1rem;
  }

  .post-card {
    border: 1px solid var(--rule);
    border-radius: 1rem;
    padding: 1.25rem;
    background: rgba(255, 255, 255, 0.03);
  }

  .post-card h2 {
    margin: 0 0 0.65rem;
    color: var(--ink);
    font-size: 1.1rem;
  }

  .post-card p {
    margin: 0;
    color: var(--ink-dim);
    line-height: 1.6;
  }

  @media (min-width: 300px) {
    .page-shell {
      padding-right: 1rem;
      padding-left: 1rem;
      text-align: left;
    }
    /* Changes layout to a horizontal row on tablets */
    .stats-grid {
      flex-direction: row;
    }
  }

  @media (min-width: 1024px) {
    .page-shell {
      padding: 1.1rem 1rem 2rem;
      max-width: 1200px;
      margin: 0 auto;
    }
  }
</style>
